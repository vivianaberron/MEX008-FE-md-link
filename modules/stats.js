const stats = arr => {
    return `Total : ${arr.length}`;
    // console.log(total)
    // let ok=0;
    // let fail=0;
    // arr.forEach(obj => {
    //     if(obj.status ==="ok") ok++
    //     else fail++
    // });
    // console.log(ok)
    // console.log(fail)  
}

module.exports = stats;